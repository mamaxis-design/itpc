# 静的サイト（SSG）を Cloudflare Workers で配信
# Build: npm run build → Deploy: npx wrangler deploy
name = "itpc"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
not_found_handling = "404-page"
